---
title: "第1章：データエージェントの設計 ― 正しさとサイズの分離"
date: 2026-09-13
type: publication
tags: [ai-agent, design, data-layer, medallion-architecture]
status: draft
ai-reviewed: false
---

# 第1章：データエージェントの設計 ― 正しさとサイズの分離

## ―「意味的に正しいデータ」は、エージェントに渡すには大きすぎる

> **骨格（アウトライン）段階。本文執筆は operator の GO 後に着手する。**

---

## 1.1 既存アーキテクチャ ― Medallion Architecture と Gold レイヤー

データレイクの標準設計である Medallion Architecture は、データを三層で管理する：

- **Bronze** — 生データ。外部から取り込んだまま、変換なし
- **Silver** — クレンジング・正規化済み。形式は整っているが意味は未整理
- **Gold** — ビジネス意味が付与された最終レイヤー。「何が正しいか」が確定している

Gold レイヤーはデータの**意味的正しさ**の終着点として機能する。ここまでが既存の標準。

## 1.2 それでも生じる問題 ― データエージェントはなぜ実行不能になるか

Gold が正しくても、エージェントは SQL クエリの性質によって実行不能になる。理由は複数ある：

- **行数の爆発** — 結合やフルスキャンを含むクエリが巨大な結果セットを返す
- **コンテキスト超過** — トークン上限を超えた時点でパイプライン全体が止まる
- **ページングの欠如** — 集約や絞り込みなしにデータをそのまま返すクエリ設計
- **（症状として）サイレント切り詰め** — エラーが出ず「成功」が返るが、データは途中で切られている

「どの SQL を投げるか」によって実行可否が変わる。データ層の正しさとは独立した問題である。

## 1.3 新アーキテクチャ ― Platinum：正しさとサイズの分離

### 1.3.1 核心命題 ― 正しさとサイズを、別の関心として分離する

意味的に正しい Gold レイヤーは、エージェントのコンテキストに対して大きすぎる。サイズの問題を Gold の外に出す（Platinum が引き受ける）ことで、Gold は「何が正しいか」だけに集中できる。古典的な関心の分離（separation of concerns）を、エージェントのコンテキスト長という新しい制約に適用したもの。

### 1.3.2 Platinum は Gold の「実行時ウィンドウ」である

Platinum を永続層にしない。一つの問いのために開き、答えが出たら閉じる一時的な投影として設計する。Gold の上に積むのではなく、横に並べる。一般的な Medallion Architecture の実例（Platinum = 永続 serving hub）への反転になる。

### 1.3.3 スリム化の階段

意味損失の少ない順に削る：①行を絞る（無損失）→②列を絞る（無損失）→③加算集約で畳む（条件付き安全、Kimball 分類）→④密度・精度を落とす（最後の手段、宣言必須）。「分割して処理する」は総量を減らさないためこの階段に入らない。

### 1.3.4 非決定性を一点に封じ込める

意図の解釈（LLM）は非決定、窓を開ける処理（テンプレート化されたクエリ）は決定論的にする。探索が非決定でも、元データ（Gold）は無事に保たれる。

### 1.3.5 zero sync ― 同期しないから壊れない

Platinum は Gold を読むだけで書き戻さない。同期すべき相手が存在しないため、複数の問いが並行しても衝突しない。

### 1.3.6 短命であることの意味

短命だから元データが汚染されない。短命だから「この窓は今この問いのための窓」という意味が時間が経っても保たれる。

---

## この章のまとめ（骨格）

- **既存アーキ**: Medallion Architecture の Bronze→Silver→Gold は意味的正しさの積み上げ
- **問題**: Gold が正しくても、SQL クエリの性質でエージェントは実行不能になる（理由は複数）
- **新アーキ**: Platinum = Gold の実行時ウィンドウ。正しさとサイズを別の関心として分離する
- スリム化は意味損失の少ない軸から順に
- 非決定性は一点に封じ込める
- 同期しないから壊れない、短命だから意味が保たれる

---

**Last Updated**: 2026-09-13
